llvmlite>=0.42.0          # استخدام LLVM JIT للتنفيذ الفوري وتحسين الأداء
numpy>=1.26.0             # عمليات المصفوفات والحسابات المتقدمة
torch>=2.1.0              # دعم التعلم العميق والذكاء الاصطناعي
pytest>=8.0.0             # تشغيل الاختبارات لضمان استقرار اللغة
pycuda>=2024.1            # دعم تنفيذ العمليات على GPU عبر CUDA
pyopencl>=2023.1          # دعم OpenCL لمعالجة البيانات عبر GPU
numba>=0.59.0             # تحسين الأداء باستخدام JIT Compilation
opencv-python>=4.8.0      # معالجة الصور والفيديو
ffmpeg-python>=0.3.0      # التعامل مع تحويل الفيديوهات ومعالجتها
onnxruntime>=1.17.0       # تشغيل نماذج الذكاء الاصطناعي وتسريعها
pytorch-lightning>=2.2.0  # تحسين تدريب نماذج الذكاء الاصطناعي
scipy>=1.11.0             # دعم العمليات الرياضية المتقدمة
matplotlib>=3.8.0         # تصور البيانات والرسوم البيانية
tqdm>=4.66.0              # شريط تقدم للعمليات الطويلة
requests>=2.31.0          # عمليات HTTP لتنزيل وتحميل البيانات
flask>=3.0.0              # واجهة برمجية لتشغيل السيرفرات الصغيرة
fastapi>=0.110.0          # واجهة برمجية سريعة وحديثة للـ API
uvicorn>=0.27.0           # تشغيل تطبيقات FastAPI بسرعة عالية
